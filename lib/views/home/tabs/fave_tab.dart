import 'package:flutter/material.dart';

class FaveTab extends StatelessWidget {
  const FaveTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Favorites'),);
  }
}